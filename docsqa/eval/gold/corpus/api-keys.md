# API Keys

## Where to find your API key

Your API keys live under Settings > Developer > API keys. Click "Create key" to
generate a new secret. Treat keys like passwords and never commit them to source
control.

## Rotating keys

Use "Revoke" next to a key to disable it immediately, then create a replacement.
