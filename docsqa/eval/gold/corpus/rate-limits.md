# Rate Limits

## Default limit

The default API rate limit is 60 requests per minute per API key. Requests over
the limit receive an HTTP 429 response with a Retry-After header indicating when
to try again.

## Increasing limits

Paid plans can request higher rate limits by contacting support.
